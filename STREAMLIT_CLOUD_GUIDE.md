## 스트림릿 클라우드(Streamlit Cloud) 배포 및 환경 설정 안내

본 도서의 예제를 기반으로 제작한 애플리케이션을 외부 서비스로 공개하고자 할 때 필요한 설정 방법입니다. 로컬 PC 환경과 웹 클라우드 환경의 기술적 차이로 인해 발생하는 오류를 방지하고, 보안을 유지하기 위한 지침을 정리하였습니다.

### 0. 스트림릿 클라우드(Streamlit Cloud) 개요

스트림릿 클라우드는 GitHub 레포지토리와 연동하여 파이썬 기반 웹 애플리케이션을 배포할 수 있도록 지원하는 플랫폼입니다. 사용자가 코드를 수정하여 GitHub에 반영하면 서버에 즉시 업데이트되며, 리눅스 컨테이너 기반으로 구동됩니다.

### 1. 배포 진행 순서

스트림릿 클라우드를 이용한 서비스 배포는 아래와 같은 단계로 진행됩니다.

1. **계정 연동:** [Streamlit Cloud](https://streamlit.io/cloud)에 접속하여 GitHub 계정으로 로그인합니다.
2. **앱 생성:** 대시보드에서 `[Create app]` 버튼을 클릭합니다.
3. **레포지토리 선택:** 배포할 소스 코드가 담긴 GitHub 레포지토리와 브랜치(Branch)를 선택합니다.
4. **실행 파일 지정:** `Main file path` 항목에 애플리케이션의 진입점이 되는 파일명(예: `app.py` 또는 `main.py`)을 입력합니다.
5. **고급 설정(Advanced settings):** 배포 버튼을 누르기 전, 하단의 `[Advanced settings]`를 클릭하여 파이썬 버전(3.10 이상 권장)을 선택하고 **Secrets** 항목에 API Key 등 환경 변수를 입력합니다. (상세 내용은 3번 항목 참조)
6. **배포 완료:** `[Deploy]` 버튼을 클릭하면 빌드가 시작되며, 완료 후 고유한 URL을 통해 웹 서비스가 공개됩니다.

### 2. 의존성 패키지 관리 (requirements.txt)

클라우드 서버는 로컬 환경과 달리 운영에 필요한 라이브러리가 설치되어 있지 않은 상태에서 시작합니다.

* **작성 방법:** 프로젝트 루트 폴더에 `requirements.txt` 파일을 생성하고 사용 패키지를 나열합니다. (예: `streamlit`, `langchain-openai`)
* **주의 사항:** 윈도우 전용 라이브러리인 `pywin32` 등이 포함될 경우 리눅스 기반인 서버에서 설치 오류가 발생합니다. 배포 전 해당 항목을 반드시 제외하십시오.

### 3. 보안 및 환경 변수 설정 (Secrets)

API 키와 같은 민감한 정보가 담긴 `.env` 파일은 보안상 GitHub 공개 레포지토리에 업로드하지 않는 것이 원칙입니다.

* **설정 방법:** 배포 설정의 `[Secrets]` 메뉴에 API 키 정보를 입력합니다.
* **적용 방식:** TOML 형식(`KEY = "VALUE"`)으로 저장하면 서버 구동 시 환경 변수로 주입됩니다. 코드 내에서는 기존의 `os.getenv()` 방식을 그대로 유지하거나 `st.secrets["KEY"]`를 활용할 수 있습니다.

### 4. 데이터 저장소 및 벡터 스토어 관리

로컬 환경과 클라우드 환경은 파일 시스템 운영 방식에서 아래와 같은 차이가 있습니다.

* **스크립트 실행 구조의 차이:** 스트림릿 클라우드는 지정된 메인 실행 파일 하나만 구동합니다. 따라서 배포 전 `create_vector_db.py`와 같은 별도의 전처리 스크립트를 서버에서 미리 실행할 수 없습니다.
* **파일 시스템의 휘발성:** 클라우드 서버는 영구 저장 공간이 아닙니다. 서버 재시작이나 절전 모드 진입 시, 런타임 중에 생성된 로컬 파일(SQLite DB, FAISS 인덱스 등)은 모두 초기화되어 삭제됩니다.
* **해결 방법:**
  1. **외부 데이터베이스 서비스 연동:** 데이터 영속성이 필요한 경우 Pinecone, Supabase 등 외부 클라우드 DB를 연결하여 사용합니다.
  2. **임베딩 로직 내재화:** 메인 코드 내에 데이터 존재 여부를 확인하는 로직을 추가하고, 데이터가 없을 경우 실행 시점에 임베딩을 생성하도록 구성합니다. 이때 `st.cache_resource`를 활용하여 중복 연산을 방지하십시오.

### 5. 주요 오류 해결 방법 (FAQ)

* **Q: `ModuleNotFoundError` 에러가 발생합니다.**
 * **A:** `requirements.txt` 파일이 루트 폴더에 위치하는지, 라이브러리명의 대소문자가 정확한지 확인하십시오.


* **Q: 로컬에서는 찾을 수 있는 파일이 서버에서 `FileNotFoundError`를 일으킵니다.**
 * **A:** 리눅스 서버는 파일 경로의 대소문자를 구분합니다. 코드 내 호출 경로와 실제 파일명이 일치하는지 점검하십시오.


* **Q: 배포 후 대시보드에서 "Stopping..." 상태가 유지됩니다.**
 * **A:** 스트림릿 설정의 `Main file path`가 실제 파일명과 일치하는지 확인하십시오. 파일이 하위 폴더에 있다면 `src/app.py`와 같이 전체 경로를 기입해야 합니다.


* **Q: API 키 설정 후에도 인증 에러가 발생합니다.**
 * **A:** `Secrets` 설정 시 TOML 형식을 준수했는지, 키 이름 앞뒤에 불필요한 공백이 포함되지 않았는지 확인하십시오.
