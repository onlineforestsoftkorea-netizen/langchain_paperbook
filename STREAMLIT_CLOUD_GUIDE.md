## 스트림릿 클라우드(Streamlit Cloud) 배포 및 환경 설정 안내

본 도서의 예제를 기반으로 제작한 애플리케이션을 외부 서비스로 공개하고자 할 때 필요한 설정 방법입니다. 로컬 PC 환경과 웹 클라우드 환경의 기술적 차이로 인해 발생하는 오류를 방지하고, 보안을 유지하기 위한 지침을 정리하였습니다.

### 0. 스트림릿 클라우드(Streamlit Cloud) 개요

스트림릿 클라우드는 GitHub 레포지토리와 연동하여 파이썬 기반 웹 애플리케이션을 배포할 수 있도록 지원하는 플랫폼입니다. 사용자가 코드를 수정하여 GitHub에 반영하면 서버에 즉시 업데이트되며, 리눅스 컨테이너 기반으로 구동됩니다.

### 1. 의존성 패키지 관리 (requirements.txt)

클라우드 서버는 로컬 환경과 달리 운영에 필요한 라이브러리가 설치되어 있지 않은 상태에서 시작합니다.

* **작성 방법:** 프로젝트 루트 폴더에 `requirements.txt` 파일을 생성하고 사용 패키지를 나열합니다. (예: `streamlit`, `langchain-openai`)
* **주의 사항:** 윈도우 전용 라이브러리인 `pywin32` 등이 포함될 경우 리눅스 기반인 서버에서 설치 오류가 발생합니다. 배포 전 해당 항목을 반드시 제외하십시오.

### 2. 보안 및 환경 변수 설정 (Secrets)

API 키와 같은 민감한 정보가 담긴 `.env` 파일은 보안상 GitHub 공개 레포지토리에 업로드하지 않는 것이 원칙입니다.

* **설정 방법:** 스트림릿 관리 대시보드의 `[Settings] → [Secrets]` 메뉴에 API 키 정보를 입력합니다.
* **적용 방식:** TOML 형식(`KEY = "VALUE"`)으로 저장하면 서버 구동 시 환경 변수로 주입됩니다. 코드 내에서는 기존의 `os.getenv()` 방식을 그대로 유지하거나 `st.secrets["KEY"]`를 활용할 수 있습니다.

### 3. 데이터 저장소 및 벡터 스토어 관리

로컬 환경에서는 `create_vector_db.py`를 실행하여 DB 파일을 생성한 후 앱을 구동할 수 있지만, 스트림릿 클라우드에서는 다음의 이유로 운용 방식이 달라야 합니다.

* **스크립트 실행 구조의 차이:** 스트림릿 클라우드는 지정된 메인 실행 파일(예: `app.py`) 하나만 구동하는 시스템입니다. 배포 과정 중에 사용자가 별도의 전처리 스크립트를 서버에서 미리 실행할 수 있는 단계를 제공하지 않습니다.
* **파일 시스템의 휘발성:** 클라우드 서버는 고정된 저장 장치가 아닙니다. 서버가 재시작되거나 절전 모드로 진입할 경우, 런타임 중에 생성된 로컬 파일(SQLite DB, FAISS 인덱스 등)은 모두 초기화되어 삭제됩니다.
* **대안:** 1. **외부 데이터베이스 서비스 연동:** 데이터의 영속성이 필요한 경우 Pinecone, Supabase, MongoDB Atlas 등 외부 클라우드 DB 서비스를 연결하여 사용합니다.
2. **임베딩 로직 내재화:** 메인 코드 내에 데이터 존재 여부를 확인하는 로직을 추가하고, 데이터가 없을 경우 실행 시점에 임베딩을 생성하도록 구성합니다. 이때 `st.cache_resource`를 활용하여 매 실행마다 중복 연산이 발생하지 않도록 설정하십시오.

### 4. 주요 오류 해결 방법 (FAQ)

* **Q: `ModuleNotFoundError` 에러가 발생합니다.**
* **A:** `requirements.txt` 파일이 루트 폴더에 위치하는지, 라이브러리명의 대소문자가 정확한지 확인하십시오.


* **Q: 로컬에서는 찾을 수 있는 파일이 서버에서 `FileNotFoundError`를 일으킵니다.**
* **A:** 리눅스 서버는 파일 경로의 대소문자를 구분합니다. 코드 내 호출 경로와 실제 파일명이 대소문자까지 일치하는지 점검하십시오.


* **Q: 배포 후 대시보드에서 "Stopping..." 상태가 유지됩니다.**
* **A:** 스트림릿 설정의 `Main file path`가 GitHub에 업로드된 실제 메인 파일명과 일치하는지 확인하십시오. 파일이 하위 폴더에 있다면 `folder/app.py` 형식으로 기입해야 합니다.


* **Q: `Resource Limit` 또는 메모리 부족 에러가 납니다.**
* **A:** 무료 티어 환경에서는 사용 가능한 메모리에 제한이 있습니다. 대규모 인덱스 파일을 메모리에 직접 올리기보다 필요한 부분만 호출하는 방식을 권장합니다.


* **Q: API 키 설정 후에도 인증 에러가 발생합니다.**
* **A:** `Secrets` 설정 시 TOML 형식을 준수했는지, 키 이름에 불필요한 공백이 포함되지 않았는지 확인하십시오.



---

**이 가이드가 웹 서비스 배포 과정에서의 시행착오를 줄이는 데 도움이 되기를 바랍니다.**

---
